<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.codefundo.vote">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Vote4Us"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.codefundo.vote.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name="com.codefundo.vote.AuthSuccessScreen"/>
		<activity android:name="com.codefundo.vote.MainActivity2"/>
		<activity android:name="com.codefundo.vote.GeneratorActivity"/>
		<activity android:name="com.codefundo.vote.ReaderActivity">
        </activity>
    </application>

</manifest>